package com.storeOperation.dailychecklist.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.storeOperation.dailychecklist.entity.StoreVisitSubParameter;

public interface StoreVisitSubParameterRepository extends JpaRepository<StoreVisitSubParameter, Long> {

}
